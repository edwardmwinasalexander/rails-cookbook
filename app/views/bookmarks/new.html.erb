<h1>Add a recipe </h1>

<%= simple_form_for [@category, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :recipe, collection: Recipe.all%>
 <%= f.submit class:"btn btn-primary"%>
<% end %>

<%= link_to 'All Bookmarks', category_bookmarks_path(@category) %>

